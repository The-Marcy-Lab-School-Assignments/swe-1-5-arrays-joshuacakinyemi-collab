/* eslint-disable no-param-reassign */
const clearArr = (arr) => {
  arr = [];
  return arr
};

const getFirstItem = (array) => {
  return array[0];
};

module.exports = {
  clearArr,
  getFirstItem,
};
